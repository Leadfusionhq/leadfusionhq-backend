FROM node:18-alpine

WORKDIR /app

# Copy dependency files first
COPY package*.json ./
RUN npm install

# Copy everything else
COPY . .

# Expose Next.js dev port
EXPOSE 8080

# Enable hot reload
CMD ["npm", "run", "dev"]
